name: Unit Tests

# on:
#   push:
#     branches:
#       - master
#   pull_request:
#     branches: [master]
on: [push]

jobs:
  Jest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Run Tests
        run: |
          yarn install 
          yarn test